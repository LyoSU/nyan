# Linting, type checking and tests.
-r requirements.txt

# ruff replaces flake8 plus its bugbear/builtins/simplify/pie/pep8-naming
# plugins, with one tool and one configuration in pyproject.toml.
ruff >= 0.8
mypy >= 1.13
pre-commit >= 4.0

pytest >= 8.0
pytest-check >= 2.4

# tests/test_deployment.py читає docker-compose.yml. PyYAML і так приходить
# транзитивно з transformers, але тест, який залежить від чужої залежності,
# ламається від чужого релізу.
PyYAML >= 6.0

types-requests >= 2.32
types-tqdm >= 4.66
types-PyYAML >= 6.0
